import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    headerShop: "",
    headerHosting: "",
    headerRepair: ""
}

export const headerSlice = createSlice({
    name: "header",
    initialState,
    reducers: {
        
    }
})